# Local Development

## Prerequisites
- Go 1.24.1+
- Docker with Compose support

## Workflow
1. Start dependencies with `make up`.
2. Run `make test`.
3. Run `make run-api` for the placeholder server.
4. Stop dependencies with `make down`.
